package com.example.messagegeneratoredi.datastructure;

public abstract class Message {
    // the fields that are found in all message types
    String field1;
    // default fields to use (visible to itself and the subclasses)

    //the constructor has to have the minimal message fields for all the messages,
    // if all messages require the same mminimal message fields.


}
